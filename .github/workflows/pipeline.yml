name: pipeline

on: push

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
          run: hello.py
    build:
        needs: [test]
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - uses: actions/create-artifact
          run: hello.py
