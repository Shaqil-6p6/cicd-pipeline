name: pipeline

on: push

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - name: python script
          run: hello.py
    build:
        needs: [test]
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - name: python script
          run: hello.py
